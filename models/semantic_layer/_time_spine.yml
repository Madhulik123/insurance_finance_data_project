version: 2

models:
  - name: metricflow_time_spine
    description: "Daily time spine for MetricFlow (derived from dim_date)."
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        description: "Calendar date at day grain."
        granularity: day
